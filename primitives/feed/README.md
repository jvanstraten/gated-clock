Dummy primitive for feed-through
================================

This is a dummy primitive used to trick two routing columns into connecting to
each other. It's just two pins (`A` and `B`) at the same location with no
additional artwork.
